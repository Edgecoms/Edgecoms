/**
 * Where a partner writes to reach a person.
 *
 * Written into the text of every email rather than set as a Reply-To header:
 * the sending domain receives no mail, so the address a reader can see is the
 * one that has to work.
 */
export const PARTNER_CONTACT_EMAIL = "anurag@edgecoms.com";
